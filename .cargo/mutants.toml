# cargo-mutants settings for the quality gate (`scripts/quality.sh`).
# The gate runs one crate at a time; the database-backed crates run one mutant at a
# time because their tests share the roles of one Postgres cluster.
exclude_globs = ["**/tests/**", "**/benches/**"]
