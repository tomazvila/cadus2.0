//! HTTP adapter: the axum application, routes, and request handlers (R4 keeps model calls out).

#![cfg_attr(
    test,
    allow(
        clippy::unwrap_used,
        clippy::expect_used,
        clippy::panic,
        clippy::todo,
        clippy::unimplemented
    )
)]
