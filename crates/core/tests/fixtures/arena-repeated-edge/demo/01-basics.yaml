unit: Demo unit
course: demo
module: Demo module
topics:
  - id: a
    name: A
    difficulty: 0.1
    answer_kind: numeric
    expected_time_secs: 60
  # `b` declares the edge b -> a twice, with two weights. 1.0 keeps the maximum
  # in both encompassing maps and keeps one entry in the prerequisite set.
  - id: b
    name: B
    difficulty: 0.2
    answer_kind: numeric
    expected_time_secs: 60
    prerequisites:
      - id: a
        weight: 0.2
      - id: a
        weight: 0.7
  # `z` declares one weight-0 edge, which the reverse map keeps (parity trap 8).
  - id: z
    name: Z
    difficulty: 0.3
    answer_kind: numeric
    expected_time_secs: 60
    prerequisites:
      - id: a
        weight: 0.0
