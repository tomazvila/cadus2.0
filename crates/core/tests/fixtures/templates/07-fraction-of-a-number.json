{
  "v": 1,
  "topic_id": "fraction-of-a-number",
  "answer_kind": "numeric",
  "statement": "Compute ${p} \\times {a}$. Give an exact answer.",
  "params": {
    "p": {
      "kind": "rational",
      "num": { "low": 1, "high": 4 },
      "den": { "low": 2, "high": 5 }
    },
    "a": { "kind": "int", "low": 4, "high": 12 }
  },
  "answer_expr": "p*a",
  "solution_sketch": "Multiply ${a}$ by the numerator of ${p}$, then divide by the denominator.",
  "hints": [
    "What does the denominator of ${p}$ tell you to do with ${a}$?",
    "Multiply by the numerator first, then divide by the denominator."
  ],
  "samples": [
    { "params": { "p": "0.2", "a": 12 }, "expected": "12/5" },
    { "params": { "p": "2", "a": 4 }, "expected": "8" }
  ]
}
