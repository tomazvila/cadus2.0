{
  "v": 1,
  "topic_id": "exact-division",
  "answer_kind": "numeric",
  "statement": "Compute ${a} \\div {b}$.",
  "params": {
    "a": { "kind": "int", "low": 24, "high": 48 },
    "b": { "kind": "int", "low": 2, "high": 8 }
  },
  "constraints": [
    { "op": "divides", "left": "b", "right": "a" }
  ],
  "answer_expr": "a/b",
  "solution_sketch": "${b}$ divides ${a}$ exactly, so the quotient is a whole number.",
  "hints": [
    "How many groups of ${b}$ fit into ${a}$?",
    "The division leaves no remainder. Count the groups."
  ],
  "samples": [
    { "params": { "a": 24, "b": 8 }, "expected": "3" },
    { "params": { "a": 48, "b": 2 }, "expected": "24" }
  ]
}
