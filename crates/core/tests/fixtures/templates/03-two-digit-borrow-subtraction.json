{
  "v": 1,
  "topic_id": "two-digit-subtraction-with-borrow",
  "answer_kind": "numeric",
  "statement": "Compute ${a} - {b}$.",
  "params": {
    "a": { "kind": "int", "low": 52, "high": 61 },
    "b": { "kind": "int", "low": 23, "high": 32 }
  },
  "constraints": [
    { "op": "gt", "left": "a", "right": "b" },
    {
      "op": "lt",
      "left": { "mod": ["a", { "lit": 10 }] },
      "right": { "mod": ["b", { "lit": 10 }] }
    }
  ],
  "answer_expr": "a - b",
  "solution_sketch": "The ones digit of ${a}$ is smaller, so borrow one ten first.",
  "hints": [
    "Which column do you subtract first?",
    "The ones digit of ${a}$ is smaller than the ones digit of ${b}$. What does that force?",
    "Borrow one ten, then subtract the ones column."
  ],
  "samples": [
    { "params": { "a": 61, "b": 23 }, "expected": "38" },
    { "params": { "a": 52, "b": 27 }, "expected": "25" },
    { "params": { "a": 60, "b": 32 }, "expected": "28" }
  ]
}
