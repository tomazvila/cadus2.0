{
  "v": 1,
  "topic_id": "fractions-in-lowest-terms",
  "answer_kind": "numeric",
  "statement": "Compute ${a} \\div {b}$. Give an exact fraction in lowest terms.",
  "params": {
    "a": { "kind": "int", "low": 5, "high": 20 },
    "b": { "kind": "int", "low": 3, "high": 9 }
  },
  "constraints": [
    { "op": "coprime", "left": "a", "right": "b" }
  ],
  "answer_expr": "a/b",
  "solution_sketch": "${a}$ and ${b}$ share no factor above one, so the fraction is already in lowest terms.",
  "hints": [
    "Which factors do ${a}$ and ${b}$ share?",
    "A shared factor of one means the fraction needs no reduction."
  ],
  "samples": [
    { "params": { "a": 5, "b": 9 }, "expected": "5/9" },
    { "params": { "a": 20, "b": 3 }, "expected": "20/3" }
  ]
}
