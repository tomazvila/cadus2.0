{
  "v": 1,
  "topic_id": "divisibility-by-nine",
  "answer_kind": "numeric",
  "statement": "Compute ${a} \\div 9$.",
  "params": {
    "a": { "kind": "int", "low": 10, "high": 189 }
  },
  "constraints": [
    { "op": "eq", "left": { "digit_sum": "a" }, "right": { "lit": 9 } }
  ],
  "answer_expr": "a/9",
  "solution_sketch": "The digits of ${a}$ sum to nine, so nine divides ${a}$ exactly.",
  "hints": [
    "What do the digits of ${a}$ add up to?",
    "A digit sum of nine means nine divides the number exactly."
  ],
  "samples": [
    { "params": { "a": 18 }, "expected": "2" },
    { "params": { "a": 180 }, "expected": "20" }
  ]
}
