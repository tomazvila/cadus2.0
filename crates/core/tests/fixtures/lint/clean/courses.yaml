# Synthetic single-course curriculum used by every later phase's tests.
# 12 topics across 2 units / 2 modules, exercising every DATA_MODEL §1 schema
# feature: a weight-0 conceptual edge, a KP key-prerequisite two steps back, an
# encompassings_extra (non-ancestor) edge, a drill topic, anki_seeds (basic +
# cloze), supplemental vs core topics, varied answer_kinds, and a
# diagnostic_exemplar on every topic.
courses:
  - id: testcourse
    name: Test Course
    order: 1
    mastery_floor:
      - counting
